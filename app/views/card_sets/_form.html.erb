<%= form_for(@card_set) do |f| %>
  <% if @card_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_set.errors.count, "error") %> prohibited this card_set from being saved:</h2>

      <ul>
      <% @card_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :user_id %>

  <%= f.label :title, "Card Set Title" %>
  <%= f.text_field :title %>

  <%= f.label :label_front, "Label front" %>
  <%= f.text_field :label_front %>

  <%= f.label :label_back, "Label back" %>
  <%= f.text_field :label_back %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
